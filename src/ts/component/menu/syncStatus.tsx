import * as React from 'react';
import { I } from 'Lib';

const MenuSyncStatus = class MenuSyncStatus extends React.Component<I.Menu> {
	render () {
		return null;
	};
};

export default MenuSyncStatus;
