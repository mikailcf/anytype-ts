import * as React from 'react';
import { I } from 'Lib';

// Publishing disabled in offline-only mode
const PageMainSettingsDataPublish = (props: I.PageSettingsComponent) => {
	return null;
};

export default PageMainSettingsDataPublish;
